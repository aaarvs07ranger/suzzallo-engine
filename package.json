{
  "name": "suzzallo-root",
  "private": true,
  "scripts": {
    "build": "npm --prefix web run build -- --webpack && cp -R web/.next .next",
    "dev": "npm --prefix web run dev",
    "start": "npm --prefix web run start",
    "postinstall": "npm ci --prefix web"
  },
  "dependencies": {
    "next": "16.2.1",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  }
}
